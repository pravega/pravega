/**
 *
 *  Copyright (c) 2017 Dell Inc., or its subsidiaries.
 *
 */

include 'common', 'client','client:admin', 'server:segmentstore:contracts', 'server:segmentstore:storage', 'server:segmentstore:storage:impl', 'server:segmentstore:service', 'server:segmentstore:service:host',
        'shared:controller', 'server:controller:service', 'connectors:flink','shared:protocol',
        'test:integration', 'test:common', 'benchmark', 'singlenode', 'test:system', 'shared', 'shared:metrics'

